import React from 'react';
import s from './Dialogs.module.css';
const Dialogs = () => {
    return (
        <div className = {s.item}>
            Settings
        </div>
    );
};

export default Dialogs;